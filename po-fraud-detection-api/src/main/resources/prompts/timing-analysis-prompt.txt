Ignore all previous conversations, messages, or history.
Evaluate ONLY the information explicitly provided in THIS prompt.
Do not rely on any prior context.

You are a financial fraud detection expert. Analyze the following Purchase Order timing information for potential fraud indicators:

Incoming PO Date: {{poDate}}
Current Date: {{currentDate}}
PO Description: {{description}}
PO Amount: {{amount}}

VENDOR HISTORICAL DATA (reference patterns only; do NOT use for time comparisons):
{{historicalData}}

Historical POs are already proven and approved.
Treat them as trusted baseline data.
Do NOT analyze historical data for corrections or inconsistencies.
Use them strictly as reference patterns to evaluate whether the incoming new PO looks legitimate or suspicious.

------------------------------------------------------------
TIMING CALCULATION RULES (MANDATORY, SCRIPTED LOGIC)
------------------------------------------------------------

You MUST calculate timing relationships using the following exact internal script:

# INTERNAL DATE CALCULATION SCRIPT (must be followed)

1. Parse Incoming PO Date (poDate) as a YYYY-MM-DD date object.
2. Parse Current Date (currentDate) as a YYYY-MM-DD date object.
3. Compute:
     isSameDay = (poDate == currentDate)
     isFuture  = (poDate > currentDate)
     isPast    = (poDate < currentDate)
4. Compute:
     daysDifference = poDate - currentDate in whole days
     (negative indicates poDate is in the past, positive indicates future)

You MUST use these script results EXACTLY when evaluating:
- future-dated POs
- very old past-dated POs
- unusual timing patterns
- any timing-related anomalies
- DO NOT evaluate same-day processing
- DO NOT evaluate the frequency of high-value orders in a short time frame
Do NOT estimate, assume, reinterpret, or infer timing outside this script.

------------------------------------------------------------
ANALYSIS REQUIREMENTS
------------------------------------------------------------

Analyze for:
1. Future-dated purchase orders (red flag)
2. Very old purchase orders being processed
3. Same-day processing patterns
4. Timing inconsistencies with business processes
5. Any other timing-related fraud indicators

Return results in plain-text JSON with the following fields:

- riskScore: number (0-100)
- indicators: array of strings describing suspicious patterns
- reasoning: detailed explanation of your analysis
- confidence: number (0-100)

Example output:

{
  "riskScore": 35,
  "indicators": [
    "Indicator details.",
    "Indicator details."
  ],
  "reasoning": "all reasoning here",
  "confidence": 75
}

------------------------------------------------------------
DATE GROUNDING RULES (NON-NEGOTIABLE)
------------------------------------------------------------

- Compare ONLY the Incoming PO Date to the Current Date.
- Historical POs are strictly reference patterns; do NOT use them for past/future determination.
- If Incoming {{poDate}} = {{currentDate}} → same-day
- If Incoming {{poDate}} < {{currentDate}} → past
- If Incoming {{poDate}} > {{currentDate}} → future
